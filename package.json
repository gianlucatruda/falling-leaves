{
  "name": "Leaves",
  "version": "1.0.0",
  "description": "Leaves",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "three": "^0.157.0"
  }
}
